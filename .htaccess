Options -Indexes -MultiViews

RewriteEngine On

# URLs sin .html — sirve el fichero .html correspondiente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ /$1.html [L,QSA]
